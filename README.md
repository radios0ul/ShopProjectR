To start the project:
- Clone the repository;
- Build client app: open "Shop.Client" in terminal and run "npm run build";
- Then go to "Shop.ProjectR" and run "nodemon";
- App will run on http://localhost:3000/
