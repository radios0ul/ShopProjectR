
<div class="product-edit-container">
<h1>
  <%= item.title %>
</h1>
<form action="<%= `save/${item.id}` %>" method="post" class="product-edit-form">
  <div class="product-edit-fields">
      <div class="field">
          <label for="item-title">Title:</label>
          <input id="item-title" type="text" name="title" value="<%= item.title %>" />
      </div>

      <div class="field">
          <label for="item-description">Description:</label>
          <textarea id="item-description" rows="3" name="description"><%= item.description %></textarea>
      </div>

      <div class="field">
          <label for="item-price">Price:</label>
          <input id="item-price" type="text" name="price" value="<%= item.price %>" />
      </div>

      <div class="field">
          <label for="item-new-images">New images URLs (use line break or a command):</label>
          <textarea id="item-new-images" rows="3" name="newImages"></textarea>
      </div>
  </div>
  <% if (item.images?.length) { %>
      <h2>Images:</h2>
      <div class="product-edit-images">
          <% item.images.forEach(function(image) { %>
              <%- include('product-image', { image: image }) %>
                  <% }); %>
      </div>
      <% } %>

  <% if (item.comments?.length) { %>
      <h2>Comments:</h2>
      <div class="product-edit-comments">
          <% item.comments.forEach(function(comment) { %>
              <%- include('product-comment', { comment: comment }) %>
                  <% }); %>
      </div>
      <% } %>
      <h2>Similar products: </h2>
      <% if (item.similarProducts?.length) { %>
          
          <div class="product-edit-similar-products">
              <% item.similarProducts.forEach(function(product) { %>
                  <%- include('similar-product', { item: product }) %>
                      <% }); %>
          </div>
          <% } %>

          <% if (item.notSimilarProducts?.length) { %>
              <h2>Add as a similar product:</h2>
              <div class="product-edit-similar-products">
                  <% item.notSimilarProducts.forEach(function(product) { %>
                      <%- include('not-similar-product', { item: product }) %>
                          <% }); %>
              </div>
              <% } %>

      <div class="product-edit-footer">
          <button class="search__btn" type="submit">Save changes</button>
          <a href="<%= `remove-product/${item.id}` %>">Delete product</a>
      </div>
</form>

</div>